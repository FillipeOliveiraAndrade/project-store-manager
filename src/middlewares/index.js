const validateName = require('./validateName');
const validateQuantity = require('./validateQuantity');
const validateProductId = require('./validateProductId');
const validateFoundProductId = require('./validateFoundProductId');
const saleValidation = require('./saleValidation');

module.exports = {
  validateName,
  validateQuantity,
  validateProductId,
  validateFoundProductId,
  saleValidation,
};